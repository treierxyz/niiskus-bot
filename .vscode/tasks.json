{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Test",
            "type": "shell",
            "command": "node .",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        },
        {
            "label": "Build Docker image",
            "type": "shell",
            "command": "mkdir -p build; sudo docker build -t treierxyz/niiskus-bot -o - . > build/niiskus-bot.tar",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}